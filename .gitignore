# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.env

# Runs / outputs
runs/*.json
!runs/.gitkeep

# Private clinical data (never commit ADNI / memory-clinic exports)
data/private/

# Fetched PubMed corpus (large; reproducible via scripts/fetch_pubmed.py)
data/corpus/

# Raw ontology downloads (large; reproducible via build_lexicon_from_ontologies.py)
# The compiled vocabulary data/lexicon/lexicon_full.json IS tracked.
data/lexicon/sources/

# LaTeX build artifacts
paper/*.aux
paper/*.log
paper/*.out
paper/*.bbl
paper/*.blg
paper/*.toc
paper/*.fls
paper/*.fdb_latexmk
paper/*.synctex.gz

# OS
.DS_Store
Thumbs.db
